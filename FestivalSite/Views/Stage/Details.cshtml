@model FestivalLibAdmin.Model.Stage

@{
    ViewBag.Title =Model.Name+" stage";
}
<section class="breadcrumb">
    @Html.MvcSiteMap().SiteMapPath()
</section>
<h2 style="color:@Model.RGBAColor; border-color:@Model.RGBAColor;">
    <img height="20" width="30" style="height:auto;width:2em;margin-bottom:-.3em;" src="@Model.Logo" />
    @Html.DisplayFor(modelItem => Model.Name)
</h2>
<section>
    @Html.Action("StageOptredensPerLineupPartial", new { stage = Model })
</section>